type Feature =  {
    title: string;
    description: string;
    // icon: React.ReactNode;
    icon: React.ElementType;
  }

 type Step = {
    number: number;
    title: string;
  }

 type Resource = {
    title: string;
    imageUrl: string;
    link: string;
  }